import React from 'react';
import './Settings.css';

const Settings = (props) => {
    return (
        <div className="settings">
            <h1> {props.title} </h1>
        </div>
        );
    }


export default Settings;